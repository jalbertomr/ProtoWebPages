<?php
	$operando1=8;
	$operando2=3;
	$suma=$operando1+$operando2;
	$resta=$operando1+$operando2;
	$producto=$operando1*$operando2;
	$cociente=$operando1/$operando2;
	$modulo=$operando1%$operando2;
	echo($operando1 & 8);
	$bit0 = ($operando1 & 1) == 1;
	$bit1 = ($operando1 & 2) == 2;
	$bit2 = ($operando1 & 4) == 4;
	$bit3 = ($operando1 & 8) == 8;
	$bitB0 = ($operando2 & 1) == 1;
	$bitB1 = ($operando2 & 2) == 2;
	$bitB2 = ($operando2 & 4) == 4;
	$bitB3 = ($operando2 & 8) == 8;
	echo ("Valor 1: ".$operando1."<BR>");
	echo ("Valor 2: ".$operando2."<BR>");
	echo ("Suma:".$suma."<BR>");
	echo ("Resta: ".$resta."<BR>");
	echo ("Producto: ".$producto."<BR>");
	echo ("Cociente: ".$cociente."<BR>");
	echo ("Resto de la división: ".$modulo."<BR>");
	echo ("Incremento de operando1: ".++$operando1."<BR>");
	echo ("Decremento de operando2: ".--$operando2."<BR>");
	echo ("bit 3,2,1,0 de operando1:".$bit3.$bit2.$bit1.$bit0."<br>");
	echo ("bit 3,2,1,0 de operando2:".$bitB3.$bitB2.$bitB1.$bitB0);
?>
